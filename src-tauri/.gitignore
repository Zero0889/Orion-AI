# Generated by Cargo / Tauri
target/
**/*.rs.bk

# Sidecar binarios (los compila el script de build)
binaries/

# Icons generados por `cargo tauri icon` desde un PNG fuente
# (los committeamos cuando se generen una vez, no antes)
