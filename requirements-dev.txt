# Dependencias solo para desarrollo / empaquetado.
# No las necesita el usuario final.

# ── Tests ────────────────────────────────────────────────────────────
pytest

# ── Lint / format / types ────────────────────────────────────────────
ruff
mypy
pre-commit

# ── Empaquetado nativo (Tauri sidecar + ejecutable standalone) ───────
pyinstaller
